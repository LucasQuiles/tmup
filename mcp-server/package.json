{
  "name": "@tmup/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && npx esbuild src/index.ts --bundle --platform=node --format=esm --external:better-sqlite3 --outfile=dist/index.js"
  },
  "dependencies": {
    "@tmup/shared": "file:../shared",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.25.76"
  }
}
